import { AadharFormatPipe } from './aadhar-format.pipe';

describe('AadharFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AadharFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
